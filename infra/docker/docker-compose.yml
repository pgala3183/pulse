# Prefer the root compose file:
#   docker compose -f ../../docker-compose.yml up --build
#   make -C ../.. up
#
# This file remains as a thin pointer for paths that still reference
# infra/docker/docker-compose.yml.
include:
  - path: ../../docker-compose.yml
